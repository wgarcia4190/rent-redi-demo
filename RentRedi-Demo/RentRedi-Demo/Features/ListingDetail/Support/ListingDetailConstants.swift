import Foundation

enum ListingDetailConstants {
    enum Demo {
        static let renterProfileUserID = "rentredi_demo_renter"
    }

    enum Storyboard {
        static let main = "Main"
        static let applyHome = "applyHomeScreen"
        static let prequalifyHome = "prequalifyHomeScreen"
    }

    enum InviteStatus {
        static let accepted = "accepted"
        static let viewed = "viewed"
    }

    enum SubmissionType {
        static let application = "application"
        static let prequalification = "prequalification"
    }
}
